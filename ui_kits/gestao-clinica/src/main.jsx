import React from 'react';
import ReactDOM from 'react-dom/client';
import '../../../styles.css';   // tokens do repositório (raiz) — fonte única
import { App } from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(<App/>);
