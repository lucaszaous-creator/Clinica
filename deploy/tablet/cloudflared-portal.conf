# Drop-in de /etc/systemd/system/cloudflared-site.service.d/portal.conf
# Só permite alcançar o socket Unix do portal; preserva o bloqueio de TCP clínico.
[Service]
SupplementaryGroups=clinica-tablet-proxy
