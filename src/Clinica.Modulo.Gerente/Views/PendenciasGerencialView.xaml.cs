using System.Windows.Controls;

namespace Clinica.Gerente.Views;

/// <summary>
/// Fila de guias pendentes na visão da direção — o mesmo semáforo do painel do
/// faturamento, sobre os mesmos serviços compartilhados.
/// </summary>
public partial class PendenciasGerencialView : UserControl
{
    public PendenciasGerencialView()
    {
        InitializeComponent();
    }
}
