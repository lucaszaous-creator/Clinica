<Window x:Class="Clinica.Desktop.Shell.Componentes.EvolucaoEnfermagemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ctrl="clr-namespace:Clinica.Desktop.Controls"
        xmlns:comp="clr-namespace:Clinica.Desktop.Shell.Componentes"
        Title="Evolução de enfermagem" Width="1140" Height="690"
        MinWidth="1000" MinHeight="670"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource Brush.Fundo}" FontFamily="Segoe UI">

    <!-- ============================================================================
         A JANELA DA SALA DE INFUSÃO (parcela 71) — a porta que a seção do posto clínico
         NÃO alcança: a folha de execução é MODAL, e `PodeMexer` apaga o painel dela
         inteiro quando a folha encerra. É aqui que se registra a reação que aparece meia
         hora depois da última bomba.

         Desde set/2026 ("uma folha, dois lados") ela não tem XAML próprio de compositor
         nem de lista: hospeda os DOIS componentes do shell — PassagemDeEnfermagemView
         (escrever) e PassagensDeEnfermagemView (reler) —, os mesmos da seção do
         Consultório e da tela Enfermagem. Corrigir um campo corrige as três portas.
         ============================================================================ -->

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolParaVisibilidade" />
        <!-- Para STRING: o conversor de bool devolve Collapsed para QUALQUER texto, e a
             mensagem nunca apareceria (a lição da parcela 61). -->
        <ctrl:TextoParaVisibilidade x:Key="TextoParaVisibilidade" />
    </Window.Resources>

    <DockPanel Margin="24">

        <!-- Identidade em LINHA de texto, nunca faixa: faixa permanente vira moldura, e
             esta janela tem uma pergunta só. O CONTEXTO (a que a passagem fica ligada)
             saiu daqui: o compositor o escreve na primeira linha dele, e duas vezes na
             mesma janela é a mesma frase lida duas vezes. -->
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <TextBlock Style="{StaticResource DialogoTitulo}" TextWrapping="Wrap"
                       Text="Evolução de enfermagem" />
            <TextBlock Text="{Binding Paciente}" TextWrapping="Wrap" Margin="0,2,0,0"
                       FontSize="{StaticResource Fonte.H3}" FontWeight="SemiBold"
                       Foreground="{StaticResource Brush.Texto.Primario}" />
        </StackPanel>

        <!-- ===================== RODAPÉ DA JANELA =====================
             Ancorado, e declarado ANTES do miolo (parcela 47 — senão o miolo come a
             barra). Ele morava no FIM da pilha do compositor: com a consulta COFEN
             aberta, a pilha pedia ~850 px numa janela de 680 e o DockPanel decepava na
             borda — o Registrar ficava INALCANÇÁVEL no monitor de 768 px do balcão
             (o print da parcela 79). A mensagem fica AO LADO dos botões que a escrevem;
             quem decide se aparece é o texto, quem decide a cor é a gravidade. -->
        <Grid DockPanel.Dock="Bottom" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Margin="0,0,12,0" VerticalAlignment="Center"
                    Visibility="{Binding Mensagem, Converter={StaticResource TextoParaVisibilidade},
                                 FallbackValue=Collapsed}">
                <Border.Style>
                    <Style TargetType="Border" BasedOn="{StaticResource AlertaInfo}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding MensagemEhErro}" Value="True">
                                <Setter Property="Background" Value="{StaticResource Brush.Erro.Suave}" />
                                <Setter Property="BorderBrush" Value="{StaticResource Brush.Erro}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="{Binding Mensagem}" TextWrapping="Wrap" />
            </Border>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Cancelar correção" MinWidth="150"
                        VerticalAlignment="Center"
                        Style="{StaticResource BotaoSecundario}"
                        Visibility="{Binding Corrigindo, Converter={StaticResource BoolParaVisibilidade}}"
                        Command="{Binding CancelarCorrecaoCommand}" />
                <Button Content="Fechar" IsCancel="True" MinWidth="100" Margin="8,0,0,0"
                        VerticalAlignment="Center"
                        Style="{StaticResource BotaoSecundario}" />
                <Button Content="{Binding RotuloDoBotao}" Margin="8,0,0,0" MinWidth="160"
                        VerticalAlignment="Center"
                        IsEnabled="{Binding PodeRegistrar}"
                        ToolTip="Grava no prontuário do paciente, com a sua identificação e o registro no conselho."
                        Command="{Binding RegistrarCommand}" />
            </StackPanel>
        </Grid>

        <!-- Escrever à ESQUERDA, reler à DIREITA — o MESMO desenho do Atendimento do
             médico (dois desenhos para a mesma pergunta no mesmo sistema é a reclamação
             da parcela 47). Em coluna, a folha do compositor é o filho que ESTICA — a
             janela maior dá espaço à folha, e a menor nunca corta botão (parcela 79). -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*" MinWidth="560" />
                <ColumnDefinition Width="*" MinWidth="340" />
            </Grid.ColumnDefinitions>

            <!-- O COMPOSITOR é o componente do shell: a tira de números, a folha que
                 preenche, o pé com intercorrência e alergia, a porta da consulta COFEN. -->
            <Border Grid.Column="0"
                    Background="{StaticResource Brush.Superficie}"
                    BorderBrush="{StaticResource Brush.Borda}" BorderThickness="1"
                    CornerRadius="{StaticResource Raio.Medio}" Padding="14">
                <comp:PassagemDeEnfermagemView />
            </Border>

            <!-- A LISTA é o outro componente: é o que se VÊ enquanto se escreve — a PA
                 de vinte minutos atrás decide se ela reduz o gotejamento. Dentro de uma
                 folha ela é a lista DAQUELA sessão (o VM decide, pela prescrição). -->
            <comp:PassagensDeEnfermagemView Grid.Column="1" Margin="16,0,0,0" />
        </Grid>
    </DockPanel>
</Window>
