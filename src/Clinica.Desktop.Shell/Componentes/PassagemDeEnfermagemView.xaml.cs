using System.Windows.Controls;

namespace Clinica.Desktop.Shell.Componentes;

/// <summary>
/// O compositor da passagem de enfermagem — um XAML para as três portas (a seção do
/// Consultório, a janela da sala de infusão e a tela Enfermagem do shell). Ver o XAML.
/// </summary>
public partial class PassagemDeEnfermagemView : UserControl
{
    public PassagemDeEnfermagemView() => InitializeComponent();
}
