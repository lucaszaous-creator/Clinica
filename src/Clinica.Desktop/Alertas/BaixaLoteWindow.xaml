<Window x:Class="Clinica.Desktop.Alertas.BaixaLoteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Baixa em lote" Height="520" Width="640"
        Icon="/Assets/Marca/icone.png"
        WindowStartupLocation="CenterOwner" FontFamily="Segoe UI"
        Background="{StaticResource Brush.Fundo}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Dar baixa em lote" Style="{StaticResource DialogoTitulo}" Margin="0,0,0,4" />
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Style="{StaticResource TextoSuave}" Margin="0"
                       Text="Informe o número da guia gerada no sistema do convênio para cada linha. Linhas sem número são ignoradas." />
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <TextBlock Text="Data da baixa:" VerticalAlignment="Center" Margin="0,0,8,0"
                           Foreground="{StaticResource Brush.Texto.Primario}" />
                <DatePicker x:Name="DpData" Width="150" />
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="Lista">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="4,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="170" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Descricao}" VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis"
                                           Foreground="{StaticResource Brush.Texto.Primario}" />
                                <TextBox Grid.Column="1" Text="{Binding NumeroGuia, UpdateSourceTrigger=PropertyChanged}"
                                         Margin="12,0,0,0" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancelar" Style="{StaticResource BotaoSecundario}" IsCancel="True" Margin="0,0,8,0" />
            <Button Content="Confirmar baixas" Click="Confirmar_Click" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
