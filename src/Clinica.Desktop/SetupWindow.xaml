<Window x:Class="Clinica.Desktop.SetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configuração inicial — Banco de dados" Height="440" Width="620"
        Icon="/Assets/Marca/icone.png"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" FontFamily="Segoe UI">
    <Border Padding="28">
        <StackPanel>
            <Image Source="/Assets/Marca/logo-cor.png" Height="40" HorizontalAlignment="Left" Margin="0,0,0,18"
                   RenderOptions.BitmapScalingMode="HighQuality" ToolTip="Clínica SemDor" />
            <TextBlock Text="Configuração do banco de dados" FontSize="22" FontWeight="Bold" Foreground="{StaticResource Brush.Texto.Primario}" />
            <TextBlock Margin="0,6,0,0" TextWrapping="Wrap" Foreground="#555"
                       Text="Cole a connection string do PostgreSQL. Você pode colar a URI da Neon (postgresql://...) — o sistema converte automaticamente. Os dados ficam salvos com segurança apenas neste computador." />

            <TextBlock Text="Connection string / URL da Neon" Margin="0,18,0,4" FontWeight="SemiBold" />
            <TextBox x:Name="TxtConexao" Height="90" AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" FontFamily="Consolas" />

            <StackPanel Orientation="Horizontal" Margin="0,14,0,0">
                <Button x:Name="BtnTestar" Content="Testar conexão" Padding="16,8" Click="BtnTestar_Click" />
                <Button x:Name="BtnSalvar" Content="Salvar e continuar" Padding="16,8" Margin="10,0,0,0"
                        Click="BtnSalvar_Click" IsEnabled="False" />
            </StackPanel>

            <Border x:Name="BoxMensagem" Margin="0,16,0,0" CornerRadius="6" Padding="12,8" Visibility="Collapsed">
                <TextBlock x:Name="TxtMensagem" TextWrapping="Wrap" />
            </Border>
        </StackPanel>
    </Border>
</Window>
